import React, {useState} from 'react';
import {
  View,
  Text,
  TextInput,
  Button,
  StyleSheet,
  TouchableOpacity,
  Image,
  SafeAreaView
} from 'react-native';

const LiveScreen = () => {
    return(
        <View>
            <Text>PostScreen</Text>
        </View>
    )
};

export default LiveScreen;